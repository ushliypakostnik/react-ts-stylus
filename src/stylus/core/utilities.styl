// Project utilities
//////////////////////////////////////////////////////
//////////////////////////////////////////////////////
//////////////////////////////////////////////////////

// Amimation class example
.pulse {
  $animations(pulse round($transition.duration * 10) infinite, fadeOn round($transition.duration * 3) forwards)
}
